
spring:
  datasource:
    username: root
    password: Aa123456
    url: jdbc:mysql://localhost:3306/manage_system?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.jdbc.Driver
    hikari:
      connection-timeout: 3000
      connection-test-query: SELECT 1
      

#pagehelper分页插件配置
pagehelper:
  helperDialect: mysql
    multipart:
     enabled: true
     max-file-size: 50MB
     max-request-size: 50MB    
      
mybatis:
  mapper-locations: classpath:mybatis/*Mapper.xml
  type-aliases-package: com.example.study_system.model

#show sql
logging:
  level:
    com.example.study_system.dao: debug

  

  
  
